services:
  - type: web
    name: fx-gym
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    buildCommand: npm run build
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fx-gym-db
          property: connectionString
      - key: NEXTAUTH_URL
        value: https://fx-gym.onrender.com
      - key: NEXTAUTH_SECRET
        value: 2983b81f54a3e1e7d4c6f9a0b3d81c2e
      - key: EMAIL_FROM
        value: "FX Gym <ultimatereviewer24@gmail.com>"
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: "465"
      - key: EMAIL_SECURE
        value: "true"
      - key: EMAIL_USER
        value: ultimatereviewer24@gmail.com
      # 🔐 Paste your 16-char Gmail App Password here (never commit actual value)
      - key: EMAIL_PASS
        value: <RENDER_DASHBOARD_INPUT_ONLY>

databases:
  - name: fx-gym-db
    region: oregon
    plan: starter